[project]
name = "jax"
version = "0.1.0"
description = "Controlled personal AI computer assistant foundation"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
jax = "jax.main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
